INSTANCE_NAME=tailnord

# IP addresses for the containers
IP_SUBNET="10.1.1.0/24"
IP_TAILSCALE="10.1.1.2"
IP_NORDVPN="10.1.1.3"
IP_PANEL="10.1.1.4"

# Your Tailscale information
TAILSCALE_AUTH_KEY=
TAILSCALE_UP_LOGIN_SERVER=

# Your NordVPN login token
NORDVPN_TOKEN=

# Which endpoint to connect to
NORDVPN_ENDPOINT=San_Francisco

# If using the regular NordVPN client
NORDVPN_TECHNOLOGY=openvpn
NORDVPN_OPENVPN_PROTOCOL=tcp

# Disconnect and reconnect after N hours
NORDVPN_RECONNECT_AFTER_HOURS=168

# ProtonVPN — required only when running the protonvpn service.
# Populate protonvpn/wireguard/ first (see README — ProtonVPN first-time setup).
#IP_PROTONVPN="10.1.1.5"
#IP_SUBNET_V6="fd00:cafe:1::/64"
#IP_VPN_V6="fd00:cafe:1::10"
# Override the default startup config (US/San_Jose/us-ca_10.conf):
#PROTONVPN_WG_CONF=/etc/wireguard/US/San_Jose/us-ca_10.conf
# Default city to connect to on startup (persisted across restarts):
#PROTONVPN_CITY=US/San_Jose
# Portal-registered WireGuard private key (account.proton.me -> Downloads ->
# WireGuard configuration -> copy the PrivateKey line). When set, the container
# uses this pre-authorized key directly and SKIPS the local-agent + certificate
# flow entirely — plain WireGuard, like the NordVPN backend, with no cert
# expiry/renewal to manage. Leave unset to use the legacy local-agent cert flow.
#
# !! IMPORTANT: use a UNIQUE key per running instance !!
# ProtonVPN's backend allows only one live WireGuard session per key and kills
# duplicates. If two devices/instances connect with the SAME key at the same
# time, the backend keeps tearing down one session and WireGuard must re-handshake
# — showing up as severe, intermittent packet loss on BOTH. Generate a separate
# WireGuard config (its own key) in the portal for every host that runs this
# stack. (This is also why frequent server-switching can self-collide: the old
# same-key session lingers on Proton's side while the new one starts.)
#PROTONVPN_WG_PRIVATE_KEY=your-portal-wireguard-private-key
